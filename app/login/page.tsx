import LoginLayout from "@/components/layouts/login/Index";

export const metadata = {
  title: "Login | Admin Dashboard",
  description: "Secure login for the admin dashboard",
};

export default function LoginPage() {
  return <LoginLayout />;
}
